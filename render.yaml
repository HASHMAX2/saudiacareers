services:
  - type: web
    name: saudiacareers-backend
    runtime: node
    region: singapore
    plan: free
    rootDir: backend
    buildCommand: cd backend && npm install && npx prisma generate
    startCommand: cd backend && node server.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: DATABASE_URL
        sync: false
      - key: DIRECT_URL
        sync: false
      - key: JWT_ACCESS_SECRET
        sync: false
      - key: JWT_REFRESH_SECRET
        sync: false
      - key: JWT_ACCESS_EXPIRES_IN
        value: 15m
      - key: JWT_REFRESH_EXPIRES_IN
        value: 7d
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_KEY
        sync: false
      - key: SUPABASE_STORAGE_BUCKET
        value: SaudiaCareers
      - key: RESEND_API_KEY
        sync: false
      - key: RESEND_FROM_EMAIL
        value: noreply@saudiacareers.com
      - key: FRONTEND_URL
        value: https://saudiacareers.com
      # Exact origins only — CORS no longer accepts a wildcard pattern (SA-01).
      # Add any new Vercel preview/project URL here explicitly before it needs
      # cross-origin cookie access; the three current live portals are listed
      # below alongside the eventual custom domains.
      - key: ALLOWED_ORIGINS
        value: https://saudiacareers.com,https://www.saudiacareers.com,https://saudiacareers-frontend.vercel.app,https://saudiacareers-employer.vercel.app,https://saudiacareers-admin.vercel.app
      - key: COOKIE_DOMAIN
        value: .saudiacareers.com
